import {WeiConverterPipe} from './wei-converter.pipe';

describe('WeiConverterPipe', () => {
    it('create an instance', () => {
        const pipe = new WeiConverterPipe();
        expect(pipe).toBeTruthy();
    });
});
